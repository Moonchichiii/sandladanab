# Package security: a version must be at least 7 days old before bun will resolve it
# (protects against freshly published malicious releases). Locked versions are unaffected.
[install]
minimumReleaseAge = 604800
exact = true
